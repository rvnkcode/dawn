{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(jq -r '.tool_input.file_path | select(endswith(\".rs\"))') && [ -n \"$FILE\" ] && cargo fmt -- \"$FILE\" || true"
          },
          {
            "type": "command",
            "command": ".claude/hooks/clippy-check.sh"
          }
        ]
      }
    ]
  },
  "enabledPlugins": {
    "rust-analyzer-lsp@claude-plugins-official": true
  }
}

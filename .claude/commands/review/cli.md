# CLI Code Review Instructions

Review the diff between the `origin/temp` branch(the default branch of the Repository) and the current branch.
Review the implementation details by comparing them to taskwarrior's code.
Review whether taskwarrior's functionality is properly implemented, focusing on the each commands.
Since implementation will proceed in the most agile units possible, review with that in mind.
taskwarrior's reference is documented in the `CLAUDE.md` file.
Also, review whether POSIX standards are being followed.
